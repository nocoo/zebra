{
  "name": "pew-monorepo",
  "version": "2.10.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "dev": "bun run --cwd packages/web dev",
    "build": "rm -f packages/*/tsconfig.tsbuildinfo && bun run --filter @pew/core build && bun run --filter @pew/web build",
    "lint": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json && tsc --noEmit -p packages/worker/tsconfig.json && tsc --noEmit -p packages/worker-read/tsconfig.json && eslint . --max-warnings=0",
    "lint:typecheck": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json && tsc --noEmit -p packages/worker/tsconfig.json && tsc --noEmit -p packages/worker-read/tsconfig.json",
    "test:e2e": "bun run scripts/run-e2e.ts",
    "test:e2e:cli": "vitest run --config vitest.e2e-cli.config.ts",
    "test:e2e:ui": "bun run scripts/run-e2e-ui.ts",
    "test:security": "bun run scripts/run-security.ts",
    "sync": "bun run build && NODE_TLS_REJECT_UNAUTHORIZED=0 bun packages/cli/dist/bin.js sync --dev",
    "sync:prod": "bun run build && bun packages/cli/dist/bin.js sync",
    "release": "bun run scripts/release.ts",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": "eslint --max-warnings=0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.1.6",
    "@playwright/test": "^1.58.2",
    "@types/bun": "^1.3.10",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^10.0.3",
    "eslint-plugin-react-hooks": "^7.0.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.0",
    "vitest": "^3.0.0"
  },
  "overrides": {
    "brace-expansion@^2": "2.0.3",
    "brace-expansion@^5": "5.0.5",
    "cookie": "^1.1.1",
    "flatted": "^3.4.2",
    "fast-xml-parser": "^5.5.8",
    "picomatch": "^4.0.4",
    "yaml": "^2.8.3"
  }
}
